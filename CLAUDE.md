# Remove.Simply

## Versioning

When building for release (`dist`, `dist:mac`, `dist:win`, `dist:linux`, `dist:all`), auto-bump the patch version in `package.json` before building. Use semver: bump patch for fixes/small changes, minor for new features, major for breaking changes.
